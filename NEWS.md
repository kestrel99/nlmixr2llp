# nlmixr2llp 0.1

* Initial package split from `nlmixr2extra`, providing `runLLP()`,
  `profileLlp()`, `profileFixed()`, control helpers, S3 methods, tests,
  and the LLP vignette as a standalone package depending on `nlmixr2utils`.
